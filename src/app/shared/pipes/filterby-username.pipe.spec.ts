import { FilterbyUsernamePipe } from './filterby-username.pipe';

describe('FilterbyUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
